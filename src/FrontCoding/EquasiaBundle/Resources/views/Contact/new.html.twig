{% extends "FrontCodingEquasiaBundle::layout.html.twig" %}

{% block breadcrumb %}
    <li>
        <a href="{{ path('contact') }}">Contact</a>
        <span class="divider"></span>
    </li>
    <li>Contact creation</li>
{% endblock %}

{% block pagetitle %}Contact creation{% endblock %}

{% block nav %}
    {% include "FrontCodingEquasiaBundle:Contact:nav.html.twig" %}
{% endblock %}

{% block content -%}

    <h1>Contact creation</h1>

    {{ form(form) }}

    <a href="{{ path('contact') }}" class="btn btn-primary btn-lg">
        Back to the list
    </a>

{% endblock %}
