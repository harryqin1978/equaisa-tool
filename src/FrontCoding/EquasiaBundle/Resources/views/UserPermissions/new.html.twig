{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserPermissions creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('userpermissions') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
