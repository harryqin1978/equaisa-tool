{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserPermissions</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Userid</th>
                <td>{{ entity.userId }}</td>
            </tr>
            <tr>
                <th>Controller</th>
                <td>{{ entity.controller }}</td>
            </tr>
            <tr>
                <th>Action</th>
                <td>{{ entity.action }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('userpermissions') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('userpermissions_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
