{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Timelog creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('timelog') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
